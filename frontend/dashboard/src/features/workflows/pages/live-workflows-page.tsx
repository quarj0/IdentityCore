"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import {Button,Card,CardContent,Input,Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from "@identitycore/ui"; import {PageHeading} from "@/components/shared/page-heading"; import {dashboardApi,Project,WorkflowDefinition} from "@/lib/dashboard-api";
const defaultSteps=["consent","document","selfie","liveness","face_match","decision","manual_review","webhook"];
export function LiveWorkflowsPage(){const [projects,setProjects]=useState<Project[]>([]),[items,setItems]=useState<WorkflowDefinition[]>([]),[project,setProject]=useState(""),[name,setName]=useState(""),[error,setError]=useState(""); const load=()=>dashboardApi.workflows(project).then(x=>setItems(x.results)).catch(e=>setError(e.message)); useEffect(()=>{dashboardApi.projects().then(x=>{setProjects(x.results);setProject(x.results[0]?.id??"")})},[]); useEffect(()=>{load()},[project]); async function create(){try{await dashboardApi.createWorkflow({project_id:project,name,description:"",steps:defaultSteps,settings:{}});setName("");await load()}catch(e){setError(e instanceof Error?e.message:"Unable to create workflow.")}} return <div className="space-y-8"><PageHeading title="Workflows" description="Build and publish versioned identity verification flows."/>{error?<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>:null}<Card><CardContent className="grid gap-3 p-5 md:grid-cols-[220px_1fr_auto]"><Select value={project} onValueChange={setProject}><SelectTrigger><SelectValue placeholder="Project"/></SelectTrigger><SelectContent>{projects.map(x=><SelectItem key={x.id} value={x.id}>{x.name}</SelectItem>)}</SelectContent></Select><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Workflow name"/><Button disabled={!project||!name.trim()} onClick={create}>Create workflow</Button></CardContent></Card><div className="grid gap-4 md:grid-cols-2">{items.map(x=><Link key={x.id} href={`/workflows/${x.id}`}><Card><CardContent className="p-6"><h2 className="font-semibold">{x.name}</h2><p className="mt-2 text-sm capitalize text-slate-500">{x.status} · version {x.current_version}</p></CardContent></Card></Link>)}</div></div>}
