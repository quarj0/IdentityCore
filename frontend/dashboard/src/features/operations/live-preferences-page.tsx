"use client"; import {useEffect,useState} from "react"; import {Button,Card,CardContent,Switch} from "@identitycore/ui"; import {PageHeading} from "@/components/shared/page-heading"; import {dashboardApi} from "@/lib/dashboard-api";
const labels:{[key:string]:string}={email_verification_updates:"Email verification updates",email_security_alerts:"Email security alerts",email_webhook_failures:"Email webhook failures",in_app_verification_updates:"In-app verification updates",in_app_security_alerts:"In-app security alerts"};export function LivePreferencesPage(){const [values,setValues]=useState<Record<string,boolean>>({}),[message,setMessage]=useState("");useEffect(()=>{dashboardApi.notificationPreferences().then(x=>setValues(x.preferences))},[]);async function save(){try{const x=await dashboardApi.updateNotificationPreferences(values);setValues(x.preferences);setMessage("Preferences saved.")}catch(e){setMessage(e instanceof Error?e.message:"Save failed.")}}return <div className="space-y-8"><PageHeading title="Notification settings" description="Choose supported email and in-app alerts."/><Card><CardContent className="space-y-5 p-6">{Object.entries(labels).map(([key,label])=><label className="flex items-center justify-between" key={key}><span>{label}</span><Switch checked={!!values[key]} onCheckedChange={v=>setValues({...values,[key]:v})}/></label>)}<Button onClick={save}>Save preferences</Button>{message?<p className="text-sm">{message}</p>:null}</CardContent></Card></div>}
