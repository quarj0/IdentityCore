"use client"; import {useEffect,useState} from "react"; import {Button,Card,CardContent,Input} from "@identitycore/ui"; import {PageHeading} from "@/components/shared/page-heading"; import {dashboardApi,DashboardUser} from "@/lib/dashboard-api";
export function LiveProfilePage(){const [user,setUser]=useState<DashboardUser|null>(null),[message,setMessage]=useState("");useEffect(()=>{dashboardApi.profile().then(x=>setUser(x.user))},[]);async function save(){if(!user)return;try{const x=await dashboardApi.updateProfile(user);setUser(x.user);setMessage("Profile saved.")}catch(e){setMessage(e instanceof Error?e.message:"Save failed.")}}return <div className="space-y-8"><PageHeading title="Profile" description="Update your authenticated account."/><Card><CardContent className="grid gap-4 p-6">{user?<><Input value={user.first_name} onChange={e=>setUser({...user,first_name:e.target.value})} placeholder="First name"/><Input value={user.last_name} onChange={e=>setUser({...user,last_name:e.target.value})} placeholder="Last name"/><Input value={user.phone_number} onChange={e=>setUser({...user,phone_number:e.target.value})} placeholder="Phone number"/><Button onClick={save}>Save profile</Button></>:<p>Loading...</p>}{message?<p className="text-sm">{message}</p>:null}</CardContent></Card></div>}
