"use client"; import {useEffect,useState} from "react"; import {Button,Card,CardContent,Input} from "@identitycore/ui"; import {PageHeading} from "@/components/shared/page-heading"; import {dashboardApi} from "@/lib/dashboard-api";
export function LiveDangerPage(){const [name,setName]=useState(""),[confirmation,setConfirmation]=useState(""),[message,setMessage]=useState("");useEffect(()=>{dashboardApi.organization().then(x=>setName(x.name))},[]);async function suspend(){try{await dashboardApi.suspendWorkspace(confirmation);setMessage("Workspace suspended. Sign in again after a platform administrator reactivates it.")}catch(e){setMessage(e instanceof Error?e.message:"Suspension failed.")}}return <div className="space-y-8"><PageHeading title="Danger zone" description="Suspend this workspace while retaining auditable records."/><Card className="border-red-200"><CardContent className="space-y-4 p-6"><p className="text-sm">Type <strong>{name}</strong> to confirm. Active credentials, sessions, and webhook delivery will be disabled.</p><Input value={confirmation} onChange={e=>setConfirmation(e.target.value)} aria-label="Organization name confirmation"/><Button variant="destructive" disabled={confirmation!==name} onClick={suspend}>Suspend workspace</Button>{message?<p className="text-sm">{message}</p>:null}</CardContent></Card></div>}
